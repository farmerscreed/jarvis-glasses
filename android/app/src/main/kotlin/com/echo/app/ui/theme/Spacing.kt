package com.echo.app.ui.theme

import androidx.compose.ui.unit.dp

/** 4dp soft grid from the design sheet. Screens use these — never raw dp literals. */
object JarvisSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp // gutter
    val lg = 24.dp // screen margin, block rhythm
    val xl = 40.dp
}
